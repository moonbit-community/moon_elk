///|
pub fn force_min(left : Double, right : Double) -> Double {
  if left < right {
    left
  } else {
    right
  }
}

///|
pub fn force_max(left : Double, right : Double) -> Double {
  if left > right {
    left
  } else {
    right
  }
}
